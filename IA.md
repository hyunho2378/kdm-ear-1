# IA.md
이어쉼표 앱 개선판

## 0 구조
3탭 HIG 앱. 하단 탭바. 탭 전환은 activeTab 상태(useState), React Router 불필요.
탭 홈 기록 설정.
세션 상태(인메모리, 새로고침 리셋)
- fatigue 0에서 1. 청취 중 자동 상승, 쉼으로 리셋
- listening boolean. 이어폰 감지 시뮬레이션
- resting boolean. 쉼 모드 여부
- restCount number. 오늘 쉼 선물 횟수
- protectedDays number. 지켜낸 청력 누적 일수
- restedDays 배열. 주간 달력용
- stage number. 캐릭터 성장 단계
- dayClock number. 시뮬레이션 하루 시각. 압축 타임라인 따라 자동 진행
- nightSummary boolean. 밤 정리 화면 표시 여부

## 1 자동 감지, 시작 버튼 없음
홈 진입 시 이어폰 청취가 감지된 상태로 시작한다. 사용자가 시작을 누르지 않는다. listening true면 fatigue가 압축 시간 동안 자동 상승하고 캐릭터가 실시간으로 지쳐간다. 측정값은 화면 뒤에서 돌고 캐릭터 상태로만 드러난다.
데모용. 설정 탭에 이어폰 감지 토글과 시간 빠르게를 둔다. 운영 데모 외 비노출 가능.

## 2 홈, 보이게와 쉬게
메인 화면. 상단 status-bar1.svg(safe area top). 중앙에 실시간 모핑 캐릭터 크게(화면 높이 약 40%, 상단에 붙이지 않음). 하단 탭바(safe area bottom).
홈 상태는 fatigue로 연속 구동한다. 별도 화면 전환 아님.
- fresh, fatigue 0.33 미만. 캐릭터 쌩쌩. 대사 오늘 컨디션 좋아요. 보조 지켜낸 청력 N일째.
- tiring, 0.33에서 0.8. 캐릭터 지침. 상태 이어폰 듣는 중, 연속 청취 N분(작게). 보조 캐릭터가 조금씩 지쳐가요.
- threshold, 0.8 이상. 캐릭터 먹먹 + 이명 호. 대사 나 좀 지쳤어 잠깐 쉬게 해줄래. 1차 버튼 쉼표 누르기. 2차 텍스트 지금은 어려워(무시 시 먹먹 유지, restCount 증가 없음).
쉼표 누르기 탭 → 쉼 모드.

## 3 쉼 모드, 쉬게
홈 위 오버레이. 배경 라이트에서 다크로 400ms. 캐릭터 resting에서 recovered로 서서히 밝아진다.
카운트다운 숫자 없음. 회복은 캐릭터가 밝아지는 것과 잔잔한 앰비언트(느린 호흡 펄스)로만 드러낸다.
대사 쉬는 중, 잠깐 귀를 내려놓자 → 회복되면 한결 나아졌어.
회복 완료 시 fatigue 0으로, restCount 1 증가, 라이트로 복귀해 홈 fresh. 텍스트 돌아가기 또는 자동 복귀. 하루는 계속된다. dayClock은 멈추지 않고 계속 진행한다.
데모 건너뛰기(설정 시간 빠르게 연동).

## 4 밤 정리, 자연 전환
별도 화면. 내일로 스킵 없음.
전환 시점은 자연 시점. dayClock이 늦은 시간대(예 23시 부근)에 도달하면 홈에서 밤 정리 화면으로 자동 전환한다. 사용자가 버튼을 누르지 않아도 하루가 닫힌다. 설정의 시간 빠르게로 이 시점을 당길 수 있다.
화면은 오늘 하루만 보여준다. 기록 탭의 누적과 역할이 겹치지 않게 한다.
- restCount 1 이상. 대사 오늘 N번 쉬게 해줬어. 오늘 받은 도장. 오늘 지켜낸 청력.
- restCount 0. 담담한 카피. 오늘은 못 쉬었네 정도, 채근과 비난 없음. 도장 없음.
- 잘 쉰 날이면 캐릭터가 더 건강한 모습.
닫기. 텍스트 돌아가기로 홈 복귀(같은 날 유지, 날짜 리셋 없음). 자동 다음 날 전환 없음. 전체 리셋은 설정 처음부터로만.

## 5 기록, 쌓이게
누적 전담. 오늘 요약은 밤 정리 화면이 맡고 여기는 누적만.
- 주간 달력. 7일, 쉰 날에 도장.
- 누적. 지켜낸 청력 N일째.
- 캐릭터 성장. restedDays 누적으로 stage 상승, 사는 공간이 풍성.

## 6 설정
HIG 그룹 리스트.
- 오늘의 쉼표 알림 토글
- 60-60 규칙 안내
- 이어폰 감지 토글(데모)
- 시간 빠르게(데모, dayClock과 fatigue 진행)
- 앱 정보

## 7 세 기둥 매핑
보이게 홈 캐릭터 상태. 쉬게 쉼표와 쉼 모드. 쌓이게 밤 정리와 기록 탭.

## 8 화면 밖
또래 검증 정리는 패널과 문서용. 앱 코드에 넣지 않는다.